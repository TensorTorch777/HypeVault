# Optional: synthetic images for classifier stress-testing (not needed for training).
# Install after PyTorch: pip install -r scripts/requirements_diffusion.txt
diffusers>=0.30.0
transformers>=4.40.0
accelerate>=0.33.0
safetensors>=0.4.0
